# 🐳 Mayank & Cayley's Baby Shower — Baby Caricature Booth

A one-day, ocean-themed photo booth app. Guests walk up, enter their name and
email, snap a webcam selfie, and get a Pixar/Disney-style **baby caricature**
of themselves generated by OpenAI's `gpt-image-2` — emailed to them, with a
copy always emailed to the organizer and saved locally on the booth laptop.

## How the flow works

1. **Splash** — ocean-themed welcome screen, "Dive In!"
2. **Who are you?** — name (with tap-to-select suggestions from the guest
   list) + email. Known guests get their email prefilled if it's in the list.
3. **About you** — guests on the list get their "known for" attributes
   prefilled (editable); unknown guests type their own.
4. **Snap!** — webcam capture with retake, then "Baby-fy me!"
5. **Thank you** — shown immediately; generation and delivery run in the
   background so the next guest can start right away. A pill in the corner
   shows how many caricatures are still brewing, and any failures pop up in a
   toast so you'll know if someone's photo didn't go through.

Every generated image is also saved to the `generated/` folder as a backup,
regardless of whether the email sends.

## Setup

```bash
npm install
```

### 1. Edit the guest list

Open [`lib/guests.ts`](lib/guests.ts) and replace the sample entries with your
~40 real guests: name, what they're known for, and (optionally) email so it's
prefilled for them. This is the only "database" — everything is hardcoded on
purpose.

### 2. Fill in `.env` (or `.env.local`)

| Variable | What it's for |
| --- | --- |
| `OPENAI_API_KEY` | Image generation (`gpt-image-2` by default; override with `IMAGE_MODEL`). Your OpenAI org may need to be [verified](https://platform.openai.com/settings/organization/general) to use this model. |
| `ORGANIZER_EMAIL` | You always get an email copy of every caricature, subject includes the guest's name |
| `SMTP_HOST/PORT/USER/PASS/FROM` | Email sending. Gmail works well: use an [App Password](https://myaccount.google.com/apppasswords) (regular passwords are rejected). |
| `IMAGE_QUALITY` | `low` / `medium` / `high` (default `high` — best results, ~1 min/image) |

### 3. Run it

```bash
npm run dev        # development
# or
npm run build && npm start   # for the day of the party
```

Open http://localhost:3000, put the laptop in a visible spot, and you're done.
Camera access requires **https or localhost**, so running locally on the booth
laptop is the easy path. Everything works offline-hostable — no deploy needed.

## Costs (roughly, for 40 guests)

`gpt-image-2` at high quality: roughly $0.10–0.25/image → **~$5–10 total**

## Day-of checklist

- [ ] Guest list updated in `lib/guests.ts`
- [ ] `.env` filled in — especially the Gmail App Password
- [ ] Do one full test run with your own email; you should get two emails
      (guest + organizer copy) and see the image land in `generated/`
- [ ] Laptop plugged in, screen sleep disabled, browser camera permission
      granted
